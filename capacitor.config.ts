import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hide-the-page',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
